{
  "private": true,
  "scripts": {
    "web": "npm --prefix website run dev",
    "web:build": "npm --prefix website run build",
    "mobile": "npm --prefix app run start",
    "mobile:clear": "npm --prefix app run start -- --clear"
  }
}
